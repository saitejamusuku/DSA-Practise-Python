def remove_duplicates_brutte(arr):
    n = len(arr)
    s = set()
    for num in arr:
        if num not in s:
            s.add(num)
    return s
def remove_duplicates_optimal(arr):
    n = len(arr)
    i=0
    for j in range(1,n):
        if arr[j] != arr[i]:
            arr[i+1] = arr[j]
            i+=1
    return arr[:i+1]
arr = list(map(int,input().split()))
print(remove_duplicates_optimal(arr))